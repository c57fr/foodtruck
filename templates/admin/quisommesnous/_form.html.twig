<div class="row my-2">
    <h2>{{ action }} Qui sommes nous</h2>
</div>
<div class="container">
<div class="row my-2">
<div class="card">
    {{ form_start(form) }}
        {{ form_row(form.description) }}
        
        {{form_rest(form)}}
        
        {{ form_errors(form) }}
        <button type="submit" class="btn btn-success">{{ action }}</button>
    {{ form_end(form) }}
</div>
</div>
</div>