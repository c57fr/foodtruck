{% extends 'base.html.twig' %}

{% block title %}Nos Produits{% endblock %}

{% block body %}
<h5 class="titre-categorie">Nos produits</h5>
<div class="card-marge">

  {% for categories in categorie %}
  <a href="{{ path('front_main_categ',{'id': categories.id})}}" type="button"
    class="btn-categorie">{{categories.nom}}</a>
{% endfor %}
</div>
<h5 class="titre-categorie">Nouveautés</h5>
<!-- Carousel ================================================== -->
{% for news in new %}
<div class="container-card">
<div class="card " style="width: 16rem;">
    <img src="{{ asset('/uploads/'~news.image )}}" alt="Image" class="card-img-top" alt="...">
    <div class="card-body">
        <h5 class="card-title">{{news.nom}}</h5>
        <p class="card-text">{{news.composition}}</p>
    </div>
</div>
</div>
{% endfor %}
{% include "footer.html.twig" %}
{% endblock %}

